# Template only. The real secret is created imperatively for cluster testing (Task 9)
# and committed SOPS-encrypted to waitline-infra for Flux (Task 14). Never commit
# real values here in plaintext.
apiVersion: v1
kind: Secret
metadata:
  name: flight-tracker-secrets
  namespace: flight-tracker
type: Opaque
stringData:
  influxdb-admin-user: admin
  influxdb-admin-password: CHANGE_ME_STRONG
  influxdb-token: CHANGE_ME_LONG_RANDOM_TOKEN
  grafana-admin-user: admin
  grafana-admin-password: CHANGE_ME_STRONG
