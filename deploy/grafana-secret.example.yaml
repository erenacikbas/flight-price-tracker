# Template only. Real values are SOPS-encrypted in waitline-infra as grafana-secrets.enc.yaml.
apiVersion: v1
kind: Secret
metadata:
  name: grafana-secrets
  namespace: grafana
type: Opaque
stringData:
  grafana-admin-user: admin
  grafana-admin-password: CHANGE_ME_STRONG
  influxdb-token: CHANGE_ME_INFLUX_TOKEN   # same token as flight-tracker's InfluxDB
